# Try to solve your self first  (+=, -=, *=, /=, %=, **=)
#31. num = 10 → increase num by 5 using +=
#32. price = 200 → apply discount of 50 using -=
#*33. speed = 20 → double the speed using =
#34. distance = 100 → divide by 4 using /=
#**35. count = 7 → square the count using =
#36. total = 50 → find remainder when divided by 6 using %=
#37. time = 120 → convert to hours using /=
#38. points = 30 → add 10 bonus points
#39. rating = 4.5 → subtract 0.5
#40. wallet = 500 → multiply by 1.05 (5% interest)


#31. num = 10 → increase num by 5 using +=
num =10
num += 5
print("after adding 5 to 10: ",num)

#32. price = 200 → apply discount of 50 using -=
price = 200
price -= 50
print("after discount 50 price is: ", price)

#*33. speed = 20 → double the speed using =
speed = 20
speed *= 2
print("after double the speed :", speed)

#34. distance = 100 → divide by 4 using /=
distance = 100
distance /= 4
print("after divide 4 : ", distance)

#**35. count = 7 → square the count using =
count = 7
count **= 2
print("after square : ", count)

#36. total = 50 → find remainder when divided by 6 using %=
total = 50
total %= 6
print("remainder : ", total)

#37. time = 120 → convert to hours using /=
time = 120
time /=60
print("hours : ", time)

#38. points = 30 → add 10 bonus points
points = 30
points += 10
print("adding bonus : ", points)

#39. rating = 4.5 → subtract 0.5
rating = 4.5
rating -= 0.5
print("after subtraction : ", rating)

#40. wallet = 500 → multiply by 1.05 (5% interest)
wallet = 500
wallet *= 1.05
print("after interest", wallet)
